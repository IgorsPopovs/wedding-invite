// Этот скрипт работает только в браузере
console.log('Frontend JS loaded');
/*
document.addEventListener('DOMContentLoaded', () => {
  const imgs = document.querySelectorAll('img');
  imgs.forEach(img => {
    img.style.border = '2px solid #000';
  });
});*/